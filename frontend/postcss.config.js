// Tailwind v4 uses the @tailwindcss/vite plugin (CSS-first config); no
// PostCSS config file is required for the build. This stub exists for tools
// that probe for a PostCSS config and as documentation of that decision.
export default {};
