# Development-only: target of the /api/* rewrite in next.config.ts.
# Defaults to the Go backend bind address from the architecture contract
# (server.bind:server.port -> 127.0.0.1:8090).
# Never set this from browser input; it is developer configuration.
DASHBOARD_API_TARGET=http://127.0.0.1:8090
